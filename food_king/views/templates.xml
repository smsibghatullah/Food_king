<odoo>
    <data>
      <!-- <template id="play_sound_template">
        <script type="text/javascript">
            odoo.define('food_king.play_sound', function (require) {
                "use strict";

                var ajax = require('web.ajax');

                function playSound() {
                    var audio = new Audio('/web/static/src/sounds/bell.wav');
                    audio.play();
                }

                function js_play_sound() {
                    playSound();
                }

                $(document).ready(function () {
                    js_play_sound();
                });
            });
        </script>
    </template> -->
    </data>
</odoo>
